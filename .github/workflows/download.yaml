name: YT-DLP Smart Downloader

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'YouTube Video URL'
        required: true
      quality:
        description: 'Max Resolution'
        required: true
        default: '720'
        type: choice
        options: ['1080', '720', '480']
      audio_only:
        description: 'Download Audio Only?'
        required: true
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  download-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}

      - name: Setup yt-dlp
        uses: AnimMouse/setup-yt-dlp@v3

      - name: Setup yt-dlp YouTube cookies
        uses: AnimMouse/setup-yt-dlp/cookies@v3
        with:
          cookies: ${{ secrets.YOUTUBE_COOKIES }}

      - name: Download Media
        run: |
          mkdir -p download
          # Force a known file extension and format
          if [ "${{ github.event.inputs.audio_only }}" = "true" ]; then
            # Audio only: extract audio as MP3
            yt-dlp -x --audio-format mp3 \
                   --restrict-filenames \
                   -o "download/temp_file.mp3" \
                   "${{ github.event.inputs.video_url }}"
            EXT="mp3"
          else
            # Video: remux to MP4 (fast, no re-encoding)
            yt-dlp -f "best[height<=${{ github.event.inputs.quality }}]" \
                   --remux-video mp4 \
                   --restrict-filenames \
                   -o "download/temp_file.mp4" \
                   "${{ github.event.inputs.video_url }}"
            EXT="mp4"
          fi
          echo "EXTENSION=$EXT" >> $GITHUB_ENV

      - name: Get clean video title
        run: |
          CLEAN_TITLE=$(yt-dlp --get-title --restrict-filenames "${{ github.event.inputs.video_url }}")
          echo "CLEAN_TITLE=$CLEAN_TITLE" >> $GITHUB_ENV

      - name: Process and Split
        run: |
          cd download
          TITLE="${{ env.CLEAN_TITLE }}"
          EXT="${{ env.EXTENSION }}"
          FILE="temp_file.${EXT}"
          
          # Verify download succeeded
          if [ ! -f "$FILE" ]; then
            echo "ERROR: Downloaded file not found: $FILE"
            ls -la
            exit 1
          fi
          
          mkdir -p "$TITLE"
          mv "$FILE" "$TITLE/full_media.${EXT}"
          cd "$TITLE"
          
          SIZE=$(stat -c%s "full_media.${EXT}")
          echo "File size: $SIZE bytes"
          
          if [ $SIZE -gt 99614720 ]; then
            echo "File exceeds 95MB, splitting..."
            split -b 95M "full_media.${EXT}" "${TITLE}_part_"
            rm "full_media.${EXT}"
            
            # Windows BAT merge script
            cat > merge.bat <<EOF
@echo off
echo Reassembling $TITLE...
copy /b ${TITLE}_part_* "$TITLE.$EXT"
echo Done!
pause
EOF
            
            # Linux Bash reassemble script
            cat > reassemble.sh <<EOF
#!/bin/bash
cat ${TITLE}_part_* > "$TITLE.$EXT"
EOF
            chmod +x reassemble.sh
          else
            mv "full_media.${EXT}" "$TITLE.$EXT"
          fi
          
          echo "Final files in $(pwd):"
          ls -la

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add download/
          git commit -m "Add ${{ env.CLEAN_TITLE }}" || echo "No changes to commit"
          git push
